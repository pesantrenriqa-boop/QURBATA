name: QURBATA J1 P011-P020 Audit

on:
  push:
    branches:
      - prod/j1-p011-p020-from-frozen-register
    paths:
      - '03_BOOKS/JILID-1/PAGE_REGISTER_P011-P020_FROZEN-v1.0.md'
      - 'production/vivliostyle/**'
      - '.github/workflows/qurbata-j1-p011-p020-audit.yml'
  workflow_dispatch:

jobs:
  build-pdf:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: production/vivliostyle
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: production/vivliostyle/package.json
      - name: Install dependencies
        run: npm install
      - name: Build P011-P020 audit PDF
        run: npm run build:p011-p020:pdf
      - name: Verify artifact
        run: |
          test -f dist/QURBATA-J1-P011-P020-AUDIT.pdf
          ls -lh dist/QURBATA-J1-P011-P020-AUDIT.pdf
      - name: Upload PDF
        uses: actions/upload-artifact@v4
        with:
          name: QURBATA-J1-P011-P020-AUDIT
          path: production/vivliostyle/dist/QURBATA-J1-P011-P020-AUDIT.pdf
          if-no-files-found: error
